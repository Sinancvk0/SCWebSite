@model List< SC.Models.BlogDetaills>
<section class="section bg-light">
    <div class="container">
        <div class="row align-items-stretch retro-layout">
            <div class="col-md-4">
                @foreach (var item in Model.Where(x => x.BlogCategoryId == 2))
                {
                    <a href="single.html" class="h-entry mb-30 v-height gradient">

                        <div class="featured-img" style="background-image: url('@item.ImageUrl');"></div>

                        <div class="text">
                            <span class="date">@item.DateCreated.ToString("MM/yyyy")</span>
                            <h2>@item.Name</h2>
                        </div>
                    </a>
                }

                @foreach (var item in Model.Where(x => x.BlogCategoryId == 3))
                {
                    <a href="single.html" class="h-entry v-height gradient">

                        <div class="featured-img" style="background-image: url('@item.ImageUrl');"></div>

                        <div class="text">
                            <span class="date">@item.DateCreated.ToString("MM/yyyy")</span>
                            <h2>@item.Name</h2>
                        </div>
                    </a>
                }



            </div>
            <div class="col-md-4">
                @foreach (var item in Model.Where(x => x.BlogCategoryId == 1))
                {
                    <a href="single.html" class="h-entry img-5 h-100 gradient">

                        <div class="featured-img" style="background-image: url('@item.ImageUrl');"></div>

                        <div class="text">
                            <span class="date">@item.DateCreated.ToString("MM/yyyy")</span>
                            <h2>@item.Name</h2>
                        </div>
                    </a>
                }

            </div>
            <div class="col-md-4">
                <a href="single.html" class="h-entry mb-30 v-height gradient">

                    <div class="featured-img" style="background-image: url('/blogy/images/img_3_horizontal.jpg');"></div>

                    <div class="text">
                        <span class="date">Apr. 14th, 2022</span>
                        <h2>Startup vs corporate: What job suits you best?</h2>
                    </div>
                </a>
                <a href="single.html" class="h-entry v-height gradient">

                    <div class="featured-img" style="background-image: url('~/blogy/images/img_4_horizontal.jpg');"></div>

                    <div class="text">
                        <span class="date">Apr. 14th, 2022</span>
                        <h2>Thought you loved Python? Wait until you meet Rust</h2>
                    </div>
                </a>
            </div>
        </div>
    </div>
</section>
